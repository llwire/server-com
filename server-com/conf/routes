# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Client WebSocket Endpoint
GET    /client/local				controllers.SocketClientController.local
GET    /client/remote				controllers.SocketClientController.remote
